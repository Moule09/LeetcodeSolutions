Find Numbers with Even Number of Digits
class Solution {
    public int findNumbers(int[] nums) {
        int res=0;
        int len=0;
        for(int i=0;i<nums.length;i++){
            len=String.valueOf(nums[i]).length();
            if(len%2==0){
                res++;
            }
        }
        return res;
    }
}
